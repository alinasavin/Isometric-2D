import { CustomButton } from './Hotspot';

export interface HomeContent {
  title: string;
  description: string;
  button: CustomButton;
}

export interface FooterContent {
  text: string;
  button: CustomButton;
}
